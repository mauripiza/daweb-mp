info:
  name: atenciones post
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/api/atenciones
  body:
    type: json
    data: |-
      {
        "nombrePaciente": "Maurizio",
        "apellidoPaciente": "Pizarro",
        "nombreMedico": "Doc",
        "apellidoMedico": "Milagro",
        "fechaAtencion": "2026-03-08",
        "referencia": "Dolor"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
